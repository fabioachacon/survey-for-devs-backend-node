import { AccountPostgresRepository } from './PostgresAccountRespository';
import { fakeAccountData } from './tests/data';

let sut: AccountPostgresRepository;

describe('PostgresAccountRepository', () => {
    it('should return an account on success', async () => {
        const account = await sut.create(fakeAccountData);
        expect(account).toBeTruthy();
        expect(account.name).toBe('any_name');
    });
});
